---
phase: 01-core-wizard
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [packages/core/src/generators/docker.ts, packages/core/src/generators/nginx.ts, packages/core/src/generators/terraform.ts, packages/core/src/generators/kubernetes.ts, packages/core/src/generators/index.ts, apps/web/app/api/generate/route.ts]
autonomous: true
requirements: [GEN-03]

must_haves:
  truths:
    - "Generators produce valid-looking YAML/HCL/Conf files based on StackConfiguration"
    - "API returns real generated files instead of a placeholder"
  artifacts:
    - path: "packages/core/src/generators/docker.ts"
      provides: "Docker Compose generation logic"
    - path: "packages/core/src/generators/nginx.ts"
      provides: "NGINX configuration generation logic"
    - path: "packages/core/src/generators/terraform.ts"
      provides: "Terraform HCL generation logic"
    - path: "packages/core/src/generators/kubernetes.ts"
      provides: "Kubernetes YAML generation logic"
  key_links:
    - from: "apps/web/app/api/generate/route.ts"
      to: "packages/core/src/generators/index.ts"
      via: "generation function call"
      pattern: "generateFiles\\("
---

<objective>
Implement the core generation engines for all required infrastructure types. This converts the abstract StackConfiguration into actual code that users can deploy.

Purpose: Core value delivery (IaC generation).
Output: 4 specific generators and an orchestration service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-wizard/01-01-DISCOVERY.md
@.planning/phases/01-core-wizard/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Docker Compose & NGINX Generators</name>
  <files>packages/core/src/generators/docker.ts, packages/core/src/generators/nginx.ts</files>
  <behavior>
    - Docker: Given Node.js backend and Postgres DB, produce a docker-compose.yml with both services, environment variables, and volumes. Use `yaml` library for formatting.
    - NGINX: Given a projectName and backend service name, produce a .conf file with a server block, proxy_pass to the backend, and basic security headers. Use Handlebars-like string interpolation.
  </behavior>
  <action>
    Create the generators in packages/core.
    - Docker generator uses `yaml` package to build the object structure.
    - NGINX generator uses a template string or Handlebars.
    - Export a function `generateDocker(config: StackConfiguration): string`.
    - Export a function `generateNginx(config: StackConfiguration): string`.
  </action>
  <verify>
    <automated>npm test packages/core/src/generators/docker.test.ts packages/core/src/generators/nginx.test.ts</automated>
  </verify>
  <done>Docker and NGINX generators pass unit tests with snapshot matching</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Terraform & Kubernetes Generators</name>
  <files>packages/core/src/generators/terraform.ts, packages/core/src/generators/kubernetes.ts</files>
  <behavior>
    - Terraform: Produce HCL with providers (AWS/Azure placeholder), VPC, and EC2/ECS resources depending on config.
    - Kubernetes: Produce a deployment and service YAML for the backend and frontend.
  </behavior>
  <action>
    Create the generators in packages/core.
    - Terraform: Use a JSON-to-HCL mapper or carefully crafted template strings.
    - Kubernetes: Use `yaml` package to build Manifest objects.
    - Export a function `generateTerraform(config: StackConfiguration): string`.
    - Export a function `generateKubernetes(config: StackConfiguration): string`.
  </action>
  <verify>
    <automated>npm test packages/core/src/generators/terraform.test.ts packages/core/src/generators/kubernetes.test.ts</automated>
  </verify>
  <done>Terraform and Kubernetes generators pass unit tests</done>
</task>

<task type="auto">
  <name>Task 3: Orchestration and API Integration</name>
  <files>packages/core/src/generators/index.ts, apps/web/app/api/generate/route.ts</files>
  <action>
    Create a `generateFiles` function in `packages/core/src/generators/index.ts` that:
    - Calls all individual generators based on the `infrastructure` flags in config.
    - Returns an object: { "docker-compose.yml": "...", "nginx.conf": "...", ... }.
    - Update `apps/web/app/api/generate/route.ts` to call this function and return the results in the JSON response.
  </action>
  <verify>
    <automated>curl -X POST -H "Content-Type: application/json" -d '{"projectName": "test", "backend": {"language": "nodejs", "version": "22"}, "frontend": {"framework": "nextjs", "version": "15"}, "database": {"type": "postgres", "version": "16"}, "proxy": "nginx", "infrastructure": ["terraform", "kubernetes"]}' http://localhost:3000/api/generate | grep -E "docker-compose.yml|nginx.conf|main.tf|k8s.yaml"</automated>
  </verify>
  <done>Full generation pipeline is wired and accessible via API</done>
</task>

</tasks>

<verification>
Verify that the generated files are non-empty and syntactically correct (visual check in logs or snapshots).
</verification>

<success_criteria>
- API returns a bundle of all requested files.
- Each generator output matches expected patterns for a given configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-wizard/01-02-SUMMARY.md`
</output>
