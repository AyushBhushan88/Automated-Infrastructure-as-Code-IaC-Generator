---
phase: 01-core-wizard
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [apps/web/components/Wizard.tsx, apps/web/components/CodePreview.tsx, apps/web/app/page.tsx]
autonomous: false
requirements: [GEN-01, GEN-03]

must_haves:
  truths:
    - "User can navigate multiple steps to define their stack"
    - "Generated code is displayed with syntax highlighting"
    - "Selecting different options updates the generated code (via API)"
  artifacts:
    - path: "apps/web/components/Wizard.tsx"
      provides: "Multi-step form component"
    - path: "apps/web/components/CodePreview.tsx"
      provides: "Syntax-highlighted code display"
  key_links:
    - from: "apps/web/components/Wizard.tsx"
      to: "/api/generate"
      via: "fetch call on change or submit"
      pattern: "fetch.*api/generate"
---

<objective>
Build the interactive user interface that allows users to define their infrastructure stack and view the results in real-time. This is the primary entry point for the application.

Purpose: User experience and functional delivery of Phase 1 goals.
Output: A functional multi-step wizard and a syntax-highlighted code viewer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-wizard/01-01-SUMMARY.md
@.planning/phases/01-core-wizard/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Multi-step Wizard</name>
  <files>apps/web/components/Wizard.tsx</files>
  <action>
    Create a `Wizard` component using React and Tailwind CSS.
    - Steps: 1. Project Info, 2. Frontend/Backend, 3. Database, 4. Infrastructure Options.
    - Use `react-hook-form` and `@hookform/resolvers/zod` with the `StackConfigurationSchema`.
    - Maintain state across steps.
    - Add "Next" and "Back" buttons.
  </action>
  <verify>
    <automated>npm test apps/web/components/Wizard.test.tsx</automated>
  </verify>
  <done>Wizard component allows navigating all steps and collecting data</done>
</task>

<task type="auto">
  <name>Task 2: Implement Code Preview with Shiki</name>
  <files>apps/web/components/CodePreview.tsx</files>
  <action>
    Create a `CodePreview` component.
    - Accepts an object of { filename: content }.
    - Uses `shiki` or `prism-react-renderer` for syntax highlighting (YAML, HCL, Nginx).
    - Includes a tab switcher to navigate between different files.
  </action>
  <verify>
    <automated>npm test apps/web/components/CodePreview.test.tsx</automated>
  </verify>
  <done>CodePreview correctly renders highlighted code with tab switching</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 1 integration: Wizard -> API -> Generators -> Code Preview</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the web app.
    2. Open http://localhost:3000.
    3. Fill out the wizard:
       - Name: "My Awesome App"
       - Stack: Node.js / Next.js
       - DB: Postgres
       - Infra: Terraform & K8s
    4. Verify that generated files (docker-compose, nginx, main.tf, k8s.yaml) appear in the preview area.
    5. Verify that changing options (e.g. changing DB to MySQL) updates the code.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure the wizard flow is intuitive and the code preview is readable.
</verification>

<success_criteria>
- User can select stack components in a wizard.
- User can view all requested generated files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-wizard/01-03-SUMMARY.md`
</output>
