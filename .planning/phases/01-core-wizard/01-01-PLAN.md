---
phase: 01-core-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, apps/web/package.json, packages/core/package.json, packages/core/src/schema.ts, apps/web/app/api/generate/route.ts]
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "Monorepo is initialized with apps/web and packages/core"
    - "StackConfiguration schema is defined and exported from packages/core"
    - "A placeholder generation API endpoint exists in apps/web"
  artifacts:
    - path: "packages/core/src/schema.ts"
      provides: "Zod schema for stack configuration"
      contains: "z.object"
    - path: "apps/web/app/api/generate/route.ts"
      provides: "Next.js API route for generation"
      exports: ["POST"]
  key_links:
    - from: "apps/web/app/api/generate/route.ts"
      to: "packages/core/src/schema.ts"
      via: "import for request validation"
      pattern: "import.*StackConfiguration"
---

<objective>
Initialize the project structure as a monorepo and define the core data model (schema) for infrastructure generation. This foundation allows both the UI (frontend) and the generation engine (backend) to share the same type definitions.

Purpose: Consistency between frontend and backend.
Output: Monorepo workspace, Zod schema, and placeholder API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-wizard/01-01-DISCOVERY.md

# Interfaces extracted from research (simulated)
<interfaces>
export interface StackConfiguration {
  projectName: string;
  backend: {
    language: 'nodejs' | 'python' | 'go';
    framework?: string;
    version: string;
  };
  frontend: {
    framework: 'nextjs' | 'react' | 'vue';
    version: string;
  };
  database: {
    type: 'postgres' | 'mysql' | 'mongodb';
    version: string;
  };
  proxy: 'nginx';
  infrastructure: ('terraform' | 'kubernetes')[];
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Monorepo Structure</name>
  <files>package.json, apps/web/package.json, packages/core/package.json, turbo.json</files>
  <action>
    Initialize a Turborepo-style monorepo using npm workspaces.
    - Root package.json with workspaces: ["apps/*", "packages/*"]
    - apps/web: Next.js 15+ application (React 19)
    - packages/core: Shared logic and types
    - Initialize typescript, tailwindcss in web app
    - Configure turbo.json for basic build tasks
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Monorepo structure created and buildable</done>
</task>

<task type="auto">
  <name>Task 2: Define StackConfiguration Schema</name>
  <files>packages/core/src/schema.ts, packages/core/package.json</files>
  <action>
    Define the StackConfiguration Zod schema and export TypeScript types.
    Include fields for:
    - projectName (string, min 3 chars)
    - backend (language, version)
    - frontend (framework, version)
    - database (type, version)
    - proxy (always nginx for now per discovery)
    - infrastructure (array of terraform/kubernetes)
    Use Zod for runtime validation and type inference.
  </action>
  <verify>
    <automated>node -e "require('ts-node/register'); console.log(require('./packages/core/src/schema').StackConfigurationSchema.parse({projectName: 'test', backend: {language: 'nodejs', version: '22'}, frontend: {framework: 'nextjs', version: '15'}, database: {type: 'postgres', version: '16'}, proxy: 'nginx', infrastructure: []}))"</automated>
  </verify>
  <done>Schema defined and validates correct inputs</done>
</task>

<task type="auto">
  <name>Task 3: Create Generation API Placeholder</name>
  <files>apps/web/app/api/generate/route.ts</files>
  <action>
    Create a POST /api/generate endpoint in Next.js.
    - Validates request body against StackConfigurationSchema.
    - Returns a placeholder success message { success: true, files: [] } for now.
    - Handles validation errors (400) and server errors (500).
  </action>
  <verify>
    <automated>curl -X POST -H "Content-Type: application/json" -d '{"projectName": "test", "backend": {"language": "nodejs", "version": "22"}, "frontend": {"framework": "nextjs", "version": "15"}, "database": {"type": "postgres", "version": "16"}, "proxy": "nginx", "infrastructure": []}' http://localhost:3000/api/generate</automated>
  </verify>
  <done>API endpoint validates requests and returns 200/400 appropriately</done>
</task>

</tasks>

<verification>
Ensure the monorepo is correctly linked and the API can import from packages/core.
</verification>

<success_criteria>
- Project build succeeds.
- API endpoint responds with success on valid input and error on invalid input.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-wizard/01-01-SUMMARY.md`
</output>
