---
phase: 02-static-validation
plan: 01
type: execute
wave: 1
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - packages/core/src/validation/types.ts
  - packages/core/src/validation/index.ts
  - packages/core/src/index.ts
  - apps/web/src/app/api/generate/route.ts
  - apps/web/components/CodePreview.tsx
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "User can see a 'Security & Syntax' report after generation in the UI."
    - "Insecure patterns (e.g., root user in Docker, public S3 bucket) are flagged in the report."
    - "The API returns a combined validation result from Checkov and Trivy."
  artifacts:
    - path: "packages/core/src/validation/index.ts"
      provides: "Unified validation service wrapping Checkov and Trivy"
    - path: "packages/core/src/validation/types.ts"
      provides: "Type definitions for validation results"
  key_links:
    - from: "apps/web/src/app/api/generate/route.ts"
      to: "packages/core/src/validation"
      via: "ValidationService.validate(files)"
    - from: "apps/web/components/CodePreview.tsx"
      to: "API response"
      via: "Rendering validationIssues array"
---

<objective>
Implement a Static Validator component that runs Checkov and Trivy on generated IaC (Docker, NGINX, Terraform, K8s).
Purpose: Provide users with immediate security and syntax feedback (VAL-01).
Output: Validation service in core package and updated UI/API in web app.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-static-validation/01-01-RESEARCH.md
@packages/core/src/generators/index.ts
@apps/web/src/app/api/generate/route.ts
@apps/web/components/CodePreview.tsx

<interfaces>
From packages/core/src/generators/index.ts:
```typescript
export function generateFiles(config: StackConfiguration): Record<string, string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Validation Infrastructure</name>
  <files>packages/core/src/validation/types.ts, packages/core/src/validation/index.ts, packages/core/src/index.ts</files>
  <action>
    - Create `types.ts` defining `ValidationIssue` (severity, message, file, line, code) and `ValidationResult`.
    - Create `ValidationService` in `index.ts`.
    - Implement a `validate(files: Record<string, string>)` method.
    - This method must:
      1. Create a temporary directory using `fs.mkdtemp`.
      2. Write the provided files to that directory.
      3. Run tool wrappers (Task 2).
      4. Aggregate results and clean up the directory.
    - Export the validation module from `packages/core/src/index.ts`.
  </action>
  <verify>
    <automated>Check if packages/core/src/validation/index.ts exists and exports ValidationService</automated>
  </verify>
  <done>Validation service scaffolded and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Checkov & Trivy Wrappers</name>
  <files>packages/core/src/validation/index.ts</files>
  <action>
    - Implement private methods in `ValidationService` to wrap Checkov and Trivy CLI.
    - Use `child_process.exec` to run:
      - Checkov: `checkov -d [temp_dir] -o json --quiet --no-guide`
      - Trivy: `trivy fs [temp_dir] -f json --quiet`
    - Parse the JSON output and map it to the unified `ValidationIssue[]` format.
    - Ensure errors are handled if the tools are not installed in the environment (return a warning instead of crashing).
  </action>
  <verify>
    <automated>npm test (if tests available) or manual check of JSON mapping logic</automated>
  </verify>
  <done>Wrappers successfully execute CLIs and parse results into unified format.</done>
</task>

<task type="auto">
  <name>Task 3: API & UI Integration</name>
  <files>apps/web/src/app/api/generate/route.ts, apps/web/components/CodePreview.tsx</files>
  <action>
    - Update `apps/web/src/app/api/generate/route.ts` to call `ValidationService.validate(files)` after generation.
    - Include the `validation` results in the JSON response.
    - Update `CodePreview.tsx` to receive `validation` results via props.
    - Display a "Security & Syntax" tab or overlay that lists issues, grouped by file or severity.
    - Highlight specific lines in the code preview if a `line` number is provided in the issue.
  </action>
  <verify>
    <automated>curl -X POST /api/generate ... and check if "validation" key exists in response</automated>
  </verify>
  <done>End-to-end validation feedback visible in the UI.</done>
</task>

</tasks>

<success_criteria>
- Generation response includes a validation report.
- UI displays security flags (e.g., "CKV_DOCKER_1: Ensure that a user for the container has been created").
- The system remains functional even if Checkov/Trivy are missing (graceful degradation).
</success_criteria>
