---
phase: 03-visual-feedback
plan: 01
type: execute
wave: 1
depends_on: [02-01]
files_modified: [apps/web/package.json, apps/web/components/Wizard.tsx, apps/web/src/app/page.tsx, apps/web/components/ArchitectureVisualizer/Flow.tsx, apps/web/components/ArchitectureVisualizer/InfrastructureNode.tsx, apps/web/components/ArchitectureVisualizer/layout.ts, apps/web/components/ArchitectureVisualizer/mapping.ts]
autonomous: true
requirements: [UI-01]
---

<objective>
Implement real-time architecture visualization using XyFlow (React Flow) and ELKJS.
The goal is to provide users with immediate visual feedback as they configure their infrastructure in the Wizard, showing how components like NGINX, Backend, and Databases are connected.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-feedback/03-01-RESEARCH.md
@apps/web/components/Wizard.tsx
@apps/web/src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment Setup & State Mapping Logic</name>
  <files>apps/web/package.json, apps/web/components/Wizard.tsx, apps/web/components/ArchitectureVisualizer/mapping.ts, apps/web/components/ArchitectureVisualizer/mapping.test.ts</files>
  <action>
    - Install `@xyflow/react` and `elkjs` in `apps/web`.
    - Install `vitest` and `@testing-library/react` for automated testing of the mapping logic.
    - Update `Wizard.tsx`:
      - Add `onChange?: (data: StackConfiguration) => void` to `WizardProps`.
      - Use `watch` from `react-hook-form` in a `useEffect` to call `onChange` whenever the form state changes.
    - Create `apps/web/components/ArchitectureVisualizer/mapping.ts`:
      - Implement `mapConfigToFlow(config: StackConfiguration)` which returns `{ nodes, edges }` compatible with XyFlow.
      - Logic should include: NGINX -> Frontend, NGINX -> Backend, Backend -> Database.
    - Create `apps/web/components/ArchitectureVisualizer/mapping.test.ts` to verify the logic.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run components/ArchitectureVisualizer/mapping.test.ts</automated>
  </verify>
  <done>
    Dependencies installed, Wizard emits real-time updates, and mapping logic is tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Visualization Components</name>
  <files>apps/web/components/ArchitectureVisualizer/Flow.tsx, apps/web/components/ArchitectureVisualizer/InfrastructureNode.tsx, apps/web/components/ArchitectureVisualizer/layout.ts</files>
  <action>
    - Create `InfrastructureNode.tsx`:
      - A custom XyFlow node that uses `lucide-react` icons (e.g., Server, Database, Globe).
      - Displays the technology name (e.g., "Postgres") and the component role (e.g., "Database").
    - Create `layout.ts`:
      - Use `elkjs` to calculate node positions based on the flow structure.
      - Ensure the layout is clean (left-to-right or top-to-bottom).
    - Create `Flow.tsx`:
      - Use `@xyflow/react` to render the diagram.
      - Integrate the `layout.ts` logic via a custom hook or `useEffect` that runs when nodes/edges change.
      - Handle Next.js 15 No-SSR by using a "mounted" state check or ensuring it's used with `dynamic` in the parent.
  </action>
  <verify>
    <automated>ls apps/web/components/ArchitectureVisualizer/Flow.tsx apps/web/components/ArchitectureVisualizer/InfrastructureNode.tsx apps/web/components/ArchitectureVisualizer/layout.ts</automated>
  </verify>
  <done>
    Visualization components are implemented with auto-layout support and custom node styling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration and UI Polish</name>
  <files>apps/web/src/app/page.tsx</files>
  <action>
    - Update `apps/web/src/app/page.tsx`:
      - Maintain `currentConfig` state from the `Wizard`.
      - Use `next/dynamic` to import `ArchitectureVisualizer` with `ssr: false`.
      - Update the right column to show a tabbed interface (or a toggle) between "Live Architecture" and "Generated Code".
      - "Live Architecture" should update in real-time as the user types/selects in the Wizard.
      - Add a "Fit View" button or ensure `fitView` is called on layout changes.
    - Ensure the UI remains responsive and the diagram scales correctly within its container.
  </action>
  <verify>
    <automated>npm run build --prefix apps/web</automated>
  </verify>
  <done>
    Visualizer is integrated into the home page, showing real-time updates as the user interacts with the Wizard.
  </done>
</task>

</tasks>

<verification>
### Phase 3 Verification

**Visual Consistency Check:**
1. Run `npm run dev` and navigate to the homepage.
2. Select different backend languages and database types in the Wizard.
3. Observe the diagram on the right updating node labels immediately.
4. Verify the connections (edges) correctly represent the flow: NGINX -> Apps -> DB.

**Automated Tests:**
- Run `cd apps/web && npx vitest run` to ensure mapping logic remains correct.
- Verify build success with `npm run build`.
</verification>

<success_criteria>
- [ ] User sees an interactive diagram of the generated infrastructure components (UI-01).
- [ ] Changes in the Wizard (e.g., Database type) are reflected in the diagram labels in real-time.
- [ ] The diagram uses `elkjs` for automated, clean positioning of nodes.
- [ ] Next.js 15 SSR issues are avoided (no hydration errors).
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-feedback/03-01-SUMMARY.md`
</output>
